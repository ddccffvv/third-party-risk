<h1>Companies</h1>

<table class="table table-bordered">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
  <% @companies.each do |company| %>
    <tr>
      <td><%= company.name %></td>
      <td><%= link_to "Show", company_path(company) %></td>
    </tr>
  <% end %>
</table>
